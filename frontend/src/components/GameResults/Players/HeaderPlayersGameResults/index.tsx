import { NameContainer } from '@components/UI/NameContainer';
import { FC } from 'react';

export const HeaderPlayersGameResults: FC = () => {
  return (
    <header>
      <NameContainer text="PLAYERS" />
    </header>
  );
};

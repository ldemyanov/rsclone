import { NameContainer } from '@components/UI/NameContainer';
import { FC } from 'react';

export const HeaderGameMode: FC = () => {
  return (
    <header>
      <NameContainer text="GAME MODE" />
    </header>
  );
};

import { Pencil } from '@components/SVG/Pencil';
import { Eraser } from '@components/SVG/Eraser';
import { Square } from '@components/SVG/Square';
import { Circle } from '@components/SVG/Circle';
import { Line } from '@components/SVG/Line';
import { Pipette } from '@components/SVG/Pipette';
import { Undo } from '@components/SVG/Undo';
import { Redo } from '@components/SVG/Redo';

export const drawToolsArray = [Pencil, Eraser, Square, Circle, Line, Pipette, Undo, Redo];
export const triknesses = [4, 8, 12, 16, 20];
